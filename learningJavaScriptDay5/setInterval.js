/**
 * 
Executes a function or code snippet repeatedly at a specified interval.
Useful for:
Creating animations that update the screen at regular intervals.
Building clocks or timers that update continuously.
Polling a server for updates at regular intervals.
 */

const Array1 = [1,2,3,4,5,6,6,7,8,8];
function ThisIsArray(Arr){
    console.log(Arr);
}

 setInterval(()=> ThisIsArray(Array1),1000);

debugger;


console.log('hello');

// clearInterval(aba);